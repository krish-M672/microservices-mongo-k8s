apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-inventory
  namespace: ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-inventory
  template:
    metadata:
      labels:
        app: mongo-inventory
    spec:
      containers:
        - name: mongo
          image: mongo:7
          ports:
            - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-inventory
  namespace: ms
spec:
  selector:
    app: mongo-inventory
  ports:
    - port: 27017
      targetPort: 27017


